import { RootNavigationProps } from "@/services/navigations/types";
import { Box } from "@/shared/components";
import React from "react";

const Wallet = ({
  navigation,
}: RootNavigationProps<"Wallet">) => {


  return (
    <Box flex={1} backgroundColor={"whiteColor"}>

    </Box>
  );
};

export default Wallet;
